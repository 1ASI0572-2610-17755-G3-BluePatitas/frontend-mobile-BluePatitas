package com.bluepatitas.mobile.feature

/**
 * Feature packages are reserved for upcoming iterations.
 *
 * Current implementation only includes the development entry screen and role-based placeholders.
 * Final auth, dashboard, animals, monitoring, alerts, feeding, devices, veterinarians and settings
 * screens must be added with their own ViewModels and domain use cases when each workflow is scoped.
 */
object FeatureReadiness
